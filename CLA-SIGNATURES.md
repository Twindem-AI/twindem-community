# CLA signatures

This branch exists solely so the [CLA Assistant](https://github.com/contributor-assistant/github-action)
workflow has an **unprotected** place to store contributor signatures
(`signatures/version1/cla.json`).

Do not protect this branch and do not delete it — protecting it would break the
`CLAAssistant` status check on every pull request. Application code lives on `main`.
